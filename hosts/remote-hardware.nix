{ config, lib, pkgs, modulesPath, ... }:

{
  # Placeholder hardware config for the remote host.
  # Replace this with the generated contents of `hardware-configuration.nix` from the remote system.
}

